{
  "name": "pseudocode-agency-web",
  "version": "1.0.0",
  "description": "Pseudocode Agency website",
  "scripts": {
    "dev": "npx live-server public",
    "build": "echo 'No build step needed for static site'",
    "start": "npx live-server public"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
